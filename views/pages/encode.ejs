<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Encode Message - StegoHide</title>
    <link rel="stylesheet" href="style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/feather-icons"></script>
    <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>
</head>
<body class="bg-gray-50">
    <custom-navbar></custom-navbar>
    
    <main class="container mx-auto px-4 py-12">
        <div class="max-w-4xl mx-auto bg-white rounded-xl shadow-md overflow-hidden p-6 md:p-8">
            <h1 class="text-3xl font-bold text-gray-800 mb-2 flex items-center gap-2">
                <i data-feather="lock"></i> Hide Your Message
            </h1>
            <p class="text-gray-600 mb-8">Upload an image and enter your secret message to encode it invisibly.</p>
            
            <div class="grid md:grid-cols-2 gap-8">
                <div>
                    <h2 class="text-xl font-semibold mb-4 text-gray-700">1. Upload Image</h2>
                    <div class="border-2 border-dashed border-gray-300 rounded-lg p-6 flex flex-col items-center justify-center mb-6 cursor-pointer hover:border-indigo-300 transition-colors duration-300">
                        <input type="file" id="image-upload" class="file-input" accept="image/*" data-preview-target="image-preview">
                        <label for="image-upload" class="file-label mb-4">
                            <i data-feather="upload" class="mr-2"></i> Choose Image
                        </label>
                        <p class="text-gray-500 text-sm">Supports JPG, PNG up to 5MB</p>
                    </div>
                    <div class="relative">
                        <img id="image-preview" src="" alt="Preview" class="hidden w-full rounded-lg shadow-md">
                        <div id="image-overlay" class="absolute inset-0 bg-black bg-opacity-50 hidden items-center justify-center rounded-lg">
                            <span class="text-white font-medium">Image Preview</span>
                        </div>
                    </div>
                </div>
                
                <div>
                    <h2 class="text-xl font-semibold mb-4 text-gray-700">2. Enter Secret Message</h2>
                    <textarea id="secret-message" class="w-full border border-gray-300 rounded-lg p-4 mb-4 h-40 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 transition-colors duration-300" placeholder="Type your secret message here..."></textarea>
                    
                    <div class="mb-6">
                        <label class="flex items-center">
                            <input type="checkbox" class="rounded border-gray-300 text-indigo-600 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
                            <span class="ml-2 text-gray-700">Encrypt message with password</span>
                        </label>
                    </div>
                    
                    <button id="encode-btn" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-medium py-3 px-6 rounded-lg transition-colors duration-300 flex items-center justify-center gap-2">
                        <i data-feather="key"></i> Encode Message
                    </button>
                </div>
            </div>
            
             Result Section (Hidden by default) -->
            <!-- <div id="result-section" class="mt-12 hidden">
                <div class="border-t border-gray-200 pt-8">
                    <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center gap-2">
                        <i data-feather="check-circle" class="text-green-500"></i> Message Successfully Encoded!
                    </h2>
                    
                    <div class="grid md:grid-cols-2 gap-8">
                        <div>
                            <h3 class="text-lg font-semibold mb-3 text-gray-700">Encoded Image Preview</h3>
                            <div class="relative">
                                <img id="encoded-image" src="" alt="Encoded Image" class="w-full rounded-lg shadow-lg">
                                <div class="absolute inset-0 bg-indigo-600 opacity-0 hover:opacity-20 transition-opacity duration-300 rounded-lg"></div>
                            </div>
                        </div>
                        
                        <div>
                            <h3 class="text-lg font-semibold mb-3 text-gray-700">Download Options</h3>
                            <p class="text-gray-600 mb-6">Your secret message has been successfully hidden in the image. Download it now!</p>
                            
                            <div class="space-y-3">
                                <button class="w-full bg-green-600 hover:bg-green-700 text-white font-medium py-3 px-6 rounded-lg transition-colors duration-300 flex items-center justify-center gap-2">
                                    <i data-feather="download"></i> Download Encoded Image
                                </button>
                                <button class="w-full bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium py-3 px-6 rounded-lg transition-colors duration-300 flex items-center justify-center gap-2">
                                    <i data-feather="share-2"></i> Share Image
                                </button>
                                <button class="w-full bg-indigo-100 hover:bg-indigo-200 text-indigo-700 font-medium py-3 px-6 rounded-lg transition-colors duration-300 flex items-center justify-center gap-2">
                                    <i data-feather="lock"></i> Get Decode Link
                                </button>
                            </div>
                            
                            <div class="mt-6 p-4 bg-indigo-50 rounded-lg">
                                <h4 class="font-medium text-indigo-800 mb-2">Important Notes:</h4>
                                <ul class="list-disc list-inside text-sm text-indigo-700 space-y-1">
                                    <li>Don't modify the image after encoding</li>
                                    <li>Keep the decode password safe if you used one</li>
                                    <li>Image quality may be slightly reduced</li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <custom-footer></custom-footer>
    
    <script src="components/navbar.js"></script>
    <script src="components/footer.js"></script>
    <script src="script.js"></script>
    <script>
        feather.replace();
        
        // Demo functionality for encode button
        document.getElementById('encode-btn').addEventListener('click', function() {
            const encodeBtn = this;
            const originalText = encodeBtn.innerHTML;
            
            // Show loading state
            encodeBtn.innerHTML = '<i data-feather="loader" class="animate-spin"></i> Encoding...';
            feather.replace();
            
            // Simulate encoding process
            setTimeout(() => {
                // Show result section
                document.getElementById('result-section').classList.remove('hidden');
                
                // For demo purposes, just show the uploaded image as "encoded"
                const previewImage = document.getElementById('image-preview');
                if (previewImage.src) {
                    document.getElementById('encoded-image').src = previewImage.src;
                } else {
                    // Fallback if no image was uploaded
                    document.getElementById('encoded-image').src = 'http://static.photos/technology/640x360/42';
                }
                
                // Reset button
                encodeBtn.innerHTML = originalText;
                feather.replace();
                
                // Scroll to results
                document.getElementById('result-section').scrollIntoView({ behavior: 'smooth' });
            }, 2000);
        });
    </script>
</body> -->
<!-- </html> -->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Encode Message - StegoHide</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="style.css">
</head>
<body class="bg-light">
    <custom-navbar></custom-navbar>
    
    <main class="container py-5">
        <div class="card shadow-sm mx-auto" style="max-width: 1000px;">
            <div class="card-body p-4 p-md-5">
                <h1 class="display-6 fw-bold mb-2">
                    <i class="bi bi-lock-fill me-2"></i> Hide Your Message
                </h1>
                <p class="text-muted mb-4">Upload an image and enter your secret message to encode it invisibly.</p>
                
                <div class="row g-4">
                    <div class="col-md-6">
                        <h2 class="h5 fw-bold mb-3">1. Upload Image</h2>
                        <div class="border border-2 border-dashed rounded p-4 text-center mb-3">
                            <input type="file" id="image-upload" class="d-none" accept="image/*" data-preview-target="image-preview">
                            <label for="image-upload" class="btn btn-primary mb-3">
                                <i class="bi bi-upload me-2"></i> Choose Image
                            </label>
                            <p class="small text-muted mb-0">Supports JPG, PNG up to 5MB</p>
                        </div>
                        <div class="position-relative">
                            <img id="image-preview" src="" alt="Preview" class="d-none w-100 rounded shadow-sm">
                            <div id="image-overlay" class="position-absolute top-0 start-0 w-100 h-100 bg-dark bg-opacity-50 d-none align-items-center justify-content-center rounded">
                                <span class="text-white fw-medium">Image Preview</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <h2 class="h5 fw-bold mb-3">2. Enter Secret Message</h2>
                        <textarea id="secret-message" class="form-control mb-3" rows="6" placeholder="Type your secret message here..."></textarea>
                        
                        <!-- <div class="form-check mb-3">
                            <input type="checkbox" class="form-check-input" id="encrypt-message">
                            <label class="form-check-label" for="encrypt-message">Encrypt message with password</label>
                        </div> -->
                    </div>
                    <div class="col-md-6">
                        <div class="form-check mb-3">
                            <input type="email" class="form-check-input" id="encrypt-message">
                            <label class="form-check-label" for="encrypt-message">Enter a valid email</label>
                        </div>

                        <button id="encode-btn" class="btn btn-primary w-100">
                            <i class="bi bi-key-fill me-2"></i> Encode Message
                        </button>
                    </div>
                </div>
                
                <!-- Result Section (Hidden by default) -->
                <div id="result-section" class="mt-5 d-none">
                    <hr class="my-4">
                    <h2 class="h4 fw-bold mb-4">
                        <i class="bi bi-check-circle-fill text-success me-2"></i> Message Successfully Encoded!
                    </h2>
                    
                    <div class="row g-4">
                        <div class="col-md-6">
                            <h3 class="h6 fw-bold mb-3">Encoded Image Preview</h3>
                            <div class="position-relative">
                                <img id="encoded-image" src="" alt="Encoded Image" class="w-100 rounded shadow">
                                <div class="position-absolute top-0 start-0 w-100 h-100 bg-primary opacity-0 hover-opacity-20 rounded transition"></div>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <h3 class="h6 fw-bold mb-3">Download Options</h3>
                            <p class="text-muted mb-4">Your secret message has been successfully hidden in the image. Download it now!</p>
                            
                            <div class="d-grid gap-2">
                                <button class="btn btn-success">
                                    <i class="bi bi-download me-2"></i> Download Encoded Image
                                </button>
                                <button class="btn btn-secondary">
                                    <i class="bi bi-share me-2"></i> Share Image
                                </button>
                                <button class="btn btn-outline-primary">
                                    <i class="bi bi-lock-fill me-2"></i> Get Decode Link
                                </button>
                            </div>
                            
                            <div class="alert alert-info mt-4">
                                <h4 class="h6 fw-bold">Important Notes:</h4>
                                <ul class="small mb-0">
                                    <li>Don't modify the image after encoding</li>
                                    <li>Keep the decode password safe if you used one</li>
                                    <li>Image quality may be slightly reduced</li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <custom-footer></custom-footer>
    
    <script src="components/navbar.js"></script>
    <script src="components/footer.js"></script>
    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Demo functionality for encode button
        document.getElementById('encode-btn').addEventListener('click', function() {
            const encodeBtn = this;
            const originalText = encodeBtn.innerHTML;
            
            // Show loading state
            encodeBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Encoding...';
            
            // Simulate encoding process
            setTimeout(() => {
                // Show result section
                document.getElementById('result-section').classList.remove('d-none');
                
                // For demo purposes, just show the uploaded image as "encoded"
                const previewImage = document.getElementById('image-preview');
                if (previewImage.src) {
                    document.getElementById('encoded-image').src = previewImage.src;
                } else {
                    // Fallback if no image was uploaded
                    document.getElementById('encoded-image').src = 'http://static.photos/technology/640x360/42';
                }
                
                // Reset button
                encodeBtn.innerHTML = originalText;
                
                // Scroll to results
                document.getElementById('result-section').scrollIntoView({ behavior: 'smooth' });
            }, 2000);
        });

        // Image preview functionality
        document.getElementById('image-upload').addEventListener('change', function(e) {
            const preview = document.getElementById('image-preview');
            if (e.target.files && e.target.files[0]) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    preview.src = e.target.result;
                    preview.classList.remove('d-none');
                }
                reader.readAsDataURL(e.target.files[0]);
            }
        });
    </script>
</body>
</html>